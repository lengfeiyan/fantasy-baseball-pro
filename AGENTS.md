# AGENTS.md — 项目协作约定

本文件供 AI 编码代理（ZCode 等）在本仓库工作时自动读取并遵守。

## Git 操作约定（重要）

**代码完成后不要主动执行任何 git 写操作**——由用户决定何时提交/推送。

- 禁止主动执行：`git add`、`git commit`、`git push`、分支创建/切换、rebase、merge 等写操作
- 完成代码后只汇报：改了哪些文件、测试结果、当前有未提交改动——然后停下
- 允许只读操作：`git status` / `git log` / `git diff`（用于汇报状态）

例外：用户在当前消息中明确要求提交/推送时才执行。

## 需求面试（grilling）：大功能开工前先对齐

较大新功能（新模块、新数据源、新页面、跨多文件的改动——如 F1/F4/S3/P4a 量级）
动手前，先进入**面试模式**再进计划模式：

- 把计划建模为设计树，按轮次提问：每轮只问"前置条件已齐"的问题
- 每轮输出编号问题清单（Q1/Q2…），**每题附推荐答案**，一次等全部回答
- 答案依赖本轮其他未决问题的问题，推迟到下一轮
- 事实核查（查代码/文档/端点）自己做，不问用户；只把真正的决策摆给用户
- 结束条件：设计树无静默假设且用户确认共识；之后才写实施方案

小改动与 bug 修复不走此流程（面试有成本，对齐收益要大于成本）。
实现中发现的取舍（被否方案+理由）写入 `docs/TECHNICAL_DESIGN.md` 的决策表。

## 文档自检标准（本文件与其他 agent 文档的维护规则）

- 每条指令配**可检查的完成标准**（如"pytest 全过且新增 N 条"而非"确保质量"）
- 用正向目标表述，少用禁令；禁令只留给硬护栏且紧跟期望行为
- 环境能查到的事实（目录结构/依赖清单）不在此重复——只写查不到的约定与坑
- 单一事实来源：同一约定只在一处维护

## 项目速览

- Python 3.7+（开发环境 Anaconda），src/ 布局，包名 `fantasy_baseball`
- 运行测试：`PYTHONPATH=src python -m pytest tests/ -q`
- GUI：`python -m fantasy_baseball gui`；CLI 12 子命令
- 技术文档：`docs/TECHNICAL_DESIGN.md`（架构/算法/schema，改动核心逻辑前先读）

## 代码约定

- 注释与文档字符串用中文，说明"为什么"而非"做了什么"
- 修复 bug 时在注释标注审计背景（如"修复审计高危项"）并补回归测试
- 数据写入遵循存储策略：DB 为唯一当前数据源，CSV 仅为时间戳备份（`config.output_path/history_path/write_csv_atomic`）
- 新增抓取器遵循 data_fetch 模式：UA 头、JSON 缓存、失败返回 None 或 raise（与"无数据"区分）
- 批处理文件（*.bat）必须 GBK 编码（cmd 按系统代码页解析，UTF-8 会乱码）

## 测试完成标准

- 每个新功能/修复带回归测试；全套 pytest 通过（0 failed，当前基线 212+）且新增测试数在汇报中注明
- 用 `isolated_db` / `isolated_history` fixture 隔离真实库与备份目录
- GUI 测试用 `_process_events` 驱动真实 `_poll_queue`，勿手动分发队列回调
- Tk 相关测试偶发抖动（单个 error、重跑即过）不视为代码问题，汇报时注明
