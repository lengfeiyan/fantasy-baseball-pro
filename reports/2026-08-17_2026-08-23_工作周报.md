# 工作周报（2026-08-17 ~ 2026-08-23）

**项目**：Fantasy Baseball Pro（fantasy-baseball-pro）
**周产出**：16 个提交，+3,949 / −663 行，测试 135 → 203 个（+68），全部通过

---

## 一、代码审计与修复（本周主线）

组织了两轮全面代码审计（4 个并行审查模块 + 关键发现逐一实测验证），共发现并修复 **21 个高危问题**（含 2 个由前期修复引入的回归）：

**第一轮审计（8/17-8/18，全部清零）**
- 核心数值错误 5 个：VORP 替代水平分位数方向颠倒、SGP 缺列被当真实零产量导致投手排名倒置、价值股标记方向反、阵容验证口径错误、伤病解析三处错（复出记录不识别导致惩罚永不解除等）
- 数据完整性 3 个：user_roster 外键死循环（每次连接重建表）、配置保存跨段覆盖（实测复现：SGP 分母会改写评分权重）、蒙特卡洛 SGP 模式崩溃
- 中危全量清理：GUI 线程模型（轮询链容错、每任务独立取消信号、13 处跨线程 Tk 访问）、statcast 评分恒饱和、多源融合权重缩水、eligible_pos 死代码接线等 10 余项

**第二轮审计（8/21，高危+中危清零）**
- 高危 4 个：UTIL 位置泄漏（192/855 打者 VORP 系统性虚高，均值 +34）、xera 量级错配（投手评分再次恒饱和）、ADP 管道漏"最近一份"备份（断网时误降级示例数据）、statcast 缺字段被当最差值
- 中危 6 个：时间戳碰撞、类别平衡功能失效、FA 链路不跟随赛季配置、伤病双重惩罚、界面滚轮误关、并发写 CSV 非原子

## 二、数据架构升级：数据统一入库（8/20）

- 数据库成为唯一当前数据源：新增 ADP / 排名 / 选秀日志 / FA 推荐四张表及配套仓储，五级加载优先级（DB → 旧 CSV → 最近一份 CSV → 网络 → 示例数据）
- CSV 降级为历史备份：`output/history/` 时间戳文件永不覆盖，附"最近一份"快捷副本，兼容全部旧读取端
- 修复入库时间戳时区问题（SQLite UTC 默认差 8 小时，含存量数据一次性修正）
- 新增阵容「从最近模拟导入」一键入库功能

## 三、产品体验

- GUI：任务期间全局忙碌反馈（手表光标 + 按钮禁用防重复触发）；配置页三列布局 + 滚动支持；完成提示语同步入库语义；屏蔽插件管理入口
- CLI：新增 Windows 双击启动脚本（图形界面 + 14 项中文交互菜单），解决了批处理 GBK 编码坑
- 错误信息中文化（网络/文件/数值异常统一翻译，保留原始详情）

## 四、技术调研

- 完成 P4 联盟平台接入方案设计（ESPN/Yahoo 双平台）：实测确认 ESPN Fantasy API 可用、Yahoo 对大陆网络封锁；产出完整实施方案并存档待排期

## 下周计划

- 第二轮审计低危批次收尾（10 项，已完成待推送）
- P4a ESPN 联盟接入开发（方案已就绪）
- 打包新版 Windows exe 并更新用户文档
